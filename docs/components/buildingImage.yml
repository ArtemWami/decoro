components:
  schemas:
    BuildingImage:
      allOf:
        - $ref: '#/components/schemas/Image'
        - type: object
          properties:
            type:
              type: string
              enum: ["BUILDING_IMG"]
            buildingId:
              type: string
              format: uuid
