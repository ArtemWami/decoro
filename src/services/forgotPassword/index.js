const create = require('./create');
const resetByKey = require('./resetByKey');
const findOneByKey = require('./findOneByKey');
const destroyByUserId = require('./destroyByUserId');

module.exports = {
    create,
    resetByKey,
    findOneByKey,
    destroyByUserId,
};
